<h1>Listing users</h1>
 
<table>
  <tr>
    <th>Login</th>
    <th>Password</th>
    <th></th>
  </tr>
 
  <% @user.each do |user| %>
    <tr>
      <td><%= user.login %></td>
      <td><%= user.password %></td>
      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
  <% end %>
</table>